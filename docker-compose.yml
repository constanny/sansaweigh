version: '3.8'

services:
  mongodb:
    image: mongo:6
    container_name: sansaweigh-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7
    container_name: sansaweigh-redis
    ports:
      - "6379:6379"

volumes:
  mongo-data: